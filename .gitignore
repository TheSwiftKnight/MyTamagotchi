node_modules/
dist/
__pycache__/
*.pyc
.venv/
.venv-*/
.env
# tamagotchi.db 允许入库（团队对齐 agents 用，最终会上云）；
# 但 SQLite 的 WAL/journal 临时文件是本机写缓冲，绝不能提交
*.db-shm
*.db-wal
*.db-journal
*.sqlite3
.DS_Store

# 硬件固件构建产物（3MB，改由 Release/LFS 分发，见 frontends/hardware/README.md）
*.bin
